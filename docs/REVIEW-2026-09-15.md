# Data Review — 2026-09-15

**"The measured edge is thinner than the broker spread."**

Written after reading the gold bot's `docs/REVIEW-2026-09-15.md` (which raised
gold's `BE_TRIGGER_R` 0.30 → 0.75) and re-cutting this bot's 73 trades with the
same tooling. Entry point was `docs/HANDOFF.md` §7; this file is the review that
§7 asked for. Every number below is reproducible — nothing is typed from memory.

Previous state: 61 trades (`docs/HANDOFF.md`, 2026-09-10). Now: 73 closed +
1 open, ledger $208.74.

---

## 0. Reproduce

```bash
python3 tools/check_data.py                       # 0 fail, 61 warn
python3 tools/win_rate_report.py --spread 0.25    # baseline/eras/slices/filters/cost
python3 tools/pathwalk_sims.py --spread 0.25 --census
python3 tools/analyze_losers.py --spread 0.25
python3 tools/phantom_trades.py --spread 0.25
python3 tools/smoke_test.py                       # scenarios A–I
```

The analysis core is new this cycle: **`tools/replay_lib.py`** is the single
bar-walk implementation (direction-safe excursions, SL-first tie-break, optional
BE ratchet, horizon + TIME mark-to-market, cascade replay, ledger geometry
helpers). `pathwalk_sims.py`, `win_rate_report.py`, `analyze_losers.py` and the
rewritten `phantom_trades.py` all import it and never re-implement a walk.

**Validation:** replaying the 19 log-covered trades with the live geometry
inside their *actual* exit windows reproduces the engine's outcome **19/19**.
The walk resolves ties to the stop and the engine resolves them on ticks, so
this is the ceiling, not a given — gold's equivalent tool shipped with a SELL
excursion inversion and validated itself. `smoke_test.py` Scenario I now locks
the direction/ratchet/horizon rules so that bug class cannot return.

---

## Headline numbers

All P/L below is **gross** — the ledger has no cost model anywhere in it.

| Slice | n | W/L | WR | P/L (gross) | per trade |
|---|---|---|---|---|---|
| all rows (incl. 09-03 outliers) | 73 | 30/43 | 41.1% | −$383.10 | −$5.25 |
| from 09-05 (excl. outliers) | 71 | 30/41 | 42.3% | **+$11.74** | +$0.165 |
| live geometry (≥09-06 17:49) | 36 | 15/21 | 41.7% | +$10.52 | +$0.29 |
| new regime (post-port, ≥09-09) | 19 | 7/12 | 36.8% | +$8.73 | +$0.46 |
| last 10 | 10 | 1/9 | 10.0% | −$9.98 | −$1.00 |

At a $0.25 round trip those become −$6.01 (from 09-05) and +$3.98 (new regime)
— i.e. the cost term is the same size as the edge (§1).

True equity from $200 is **−$183.10**; the engine ledger reads $208.74 because
it resets on restarts. The gap is the two 2026-09-03 pre-port sizing outliers
(−$394.84). Excluding those, the whole forward test has made **+$11.74 gross**
in 71 trades — *before* any spread.

Mind the `1R $` values the tools print: the live-era ATR (~$85–120) means
1R ≈ $1.70–2.40, while the pre-port churn era traded 1R ≈ $0.20–0.40. Dollar
totals across eras are apples-to-oranges; R totals are only valid within one
geometry (see §8).

---

## 1. THE FINDING — the edge is smaller than the broker spread

XM's published average BTCUSD spread is **≈ $22.50–60 per 1 BTC**
([fxstreet](https://www.fxstreet.com/brokers/reviews/xm) quotes 22.50;
[gojj](https://gojj.com/review/xm/) quotes $60 ≈ 0.0727% of price). At
`LOT_SIZE = 0.01` a round trip costs **$0.225–0.60 per trade**.

| | gross / trade | cost / trade | net |
|---|---|---|---|
| from 09-05 (n=71) | +$0.165 | $0.225 … $0.60 | −$0.06 … −$0.44 |
| new regime (n=19) | +$0.46 | $0.225 … $0.60 | −$0.14 … +$0.24 |

In risk units at a typical live ATR of $85 (1R = 2×ATR×0.01 = $1.70): the spread
is **0.13–0.35R per trade**. This is why `win_rate_report.py` §8 exists —
breakeven decisive WR = (1 + s/a)/3:

| ATR $ | s=$0.10 | s=$0.25 | s=$0.50 |
|---|---|---|---|
| 10 | 50% | 75% | 117% |
| 15 | 44% | 61% | 89% |
| 30 | 39% | 47% | 61% |
| 85 | 35% | 37% | 39% |
| 150 | 34% | 36% | 39% |

The arithmetic rule: to keep the round trip at **≤15% of 1R**,

> **ATR ≥ 333 × spread$**  — i.e. ATR ≥ $73 at a $0.22 spread, ATR ≥ $200 at $0.60.

The live `MIN_ATR_PERCENT = 0.01%` is **$7.70** at $77k. It has never been a cost
control. Two consequences:

1. **Measure the real XM BTCUSD spread before anything else.** It is the single
   number that decides whether the current M5 stream is tradeable at 0.01 lot,
   and it can be read off the MT5 sidecar's tick data (or the symbol spec) in an
   afternoon. Then decide between: a lower-spread account (Ultra Low / Zero),
   a cost-ratio ATR floor as above, or fewer/larger-ATR trades.
2. A cost-ratio floor is **not** a curve-fit. Unlike the win-rate filters in
   §2–§4 it needs no within-day control — it is an identity, and it is the only
   version of "raise `MIN_ATR_PERCENT`" this data supports.

### 1b. Measured Spread & Impact (2026-09-15 live terminal measurement)

XM BTCUSD spread measured 2026-09-15 from the live terminal = **$40.00/BTC** = **$0.40 per round trip at 0.01 lot**.
Symbol `BTCUSD` exists with contract `1.0` / min lot `0.01`, while `BTCUSDm` does not exist (so `SYMBOL_MT5=BTCUSD`).

Netting that cost:
- 71 trades from 09-05 go from **+$11.74 gross** to **−$16.66 net**.
- Live era (n=19) is **+$1.13 net**, but **+$12.62** of that is 09-10 alone (without it **−$10.29 net**).

| Era / Slice | Gross P&L | Net P&L (@ $0.40/trade) | Net / Trade |
|---|---|---|---|
| from 09-05 (n=71) | +$11.74 | **−$16.66** | −$0.235 |
| live era (n=19) | +$8.73 | **+$1.13** | +$0.059 |
| live era excl 09-10 (n=16) | −$3.89 | **−$10.29** | −$0.643 |

**Cause**: M5 ATR is too small vs a $40 spread — live-era median entry ATR $84 makes cost **23.9% of 1R** with a breakeven win rate of **41.3%** vs an actual **36.8%**.

Same data re-cut per timeframe:
- **M5**: 24.0% of 1R (breakeven WR 41.3%)
- **M15**: 11.9% of 1R (breakeven WR 37.3%)
- **H1**: 5.4% of 1R (breakeven WR 35.1%)
- **H4**: 2.6% of 1R (breakeven WR 34.2%)

So the open decision is the **bar timeframe**, not BE/TP parameters, and BE/TP tuning is deferred until then.
Sample was taken at 04:06 UTC (Asian session), so the spread profile across London/NY still needs confirming.

---

## 2. The ATR% ≥ 0.06 filter is a calendar proxy — rejected on WR grounds

`win_rate_report.py` §7 now prints every candidate together with a
**within-day control**: keep-vs-skip *inside days that contain both*, so a
feature that merely selects quiet days cannot masquerade as an edge.

| ATR% ≥ 0.06 (pooled) | n | WR | P/L |
|---|---|---|---|
| keep | 52 | 44.0% | **+$22.09** |
| skip | 26 | 3.8% | −$22.63 |

| same split, own-day only | n | WR | P/L |
|---|---|---|---|
| keep | 4 | 0% | −$6.88 |
| skip | 7 | 14.3% | −$4.54 |

67 of 86 signals sit on days where the feature selects everything or nothing —
the pooled rows are mostly between-day composition (09-12/09-13 had ATR% ≈
0.038–0.048% and lost; every other day sat at 0.11–0.14%). The direction even
*flips* once the day component is removed.

The "low-ATR churn bucket" (§HANDOFF 5) is also **pre-era only**: all 33 trades
with ledger ATR% < 0.02 are 09-05/06, i.e. before the 09-06 17:49 geometry
change, and the skip log shows the floor was actually **0.04% until 09-05 22:43**
then lowered to today's 0.01% (09-06 10:52 onward). Zero live-era trades sit
near the floor. **Verdict: reject ATR-floor-by-win-rate. Use §1's cost ratio.**

---

## 3. RSI ≥ 45 (gold's adopted filter) does not transfer to BTC

| RSI ≥ 45 (pooled) | n | WR | P/L |
|---|---|---|---|
| keep | 61 | 30.5% | +$0.76 |
| skip | 17 | 29.4% | −$1.31 |

| own-day only | n | WR | P/L |
|---|---|---|---|
| keep | 50 | 22.0% | −$28.32 |
| skip | 17 | 29.4% | −$1.31 |

Pooled, the filter is a wash; within-day it is **negative**. Gold adopted
RSI ≥ 45 on gold data with an explicit criteria table — that evidence does not
exist here. **Verdict: do not port the RSI filter to BTC.**

---

## 4. Two candidates survive the within-day control — monitor, do not gate

| candidate | pooled keep | own-day keep vs skip | read |
|---|---|---|---|
| wick ratio ≤ 0.40 | 37.0% +$13.40 (n=46) | 37.0% +$13.40 vs **20.0% −$17.23** (drops 2) | holds; only 2 signals dropped, so the control is nearly the pooled number |
| entry within 0.15 ATR of EMA50 | 62.5% +$13.18 (n=8) | 62.5% +$13.18 vs 32.6% +$9.23 (n=8 vs 43) | holds, but n=8 and the CIs overlap |

`analyze_losers.py` agrees on the same direction from a different angle: on the
19 log-covered trades, winners entered **0.78 ATR from EMA50 vs 1.40 ATR for
losers**, and wick quality 62.5% (W) vs 48.5% (L). Both are consistent with
"entries taken far from the mean-reversion level are worse".

Both need **n ≥ 30 live-era** before they touch a gate. They are cheap to
instrument now: log the feature value per signal in `skipped_trades.csv`/the
dashboard counters and re-read the control next cycle.

**Conflict worth recording:** the census says SELL is the losing side within-day
(19.4% −$4.65 vs BUY 38.1% +$6.55), while the live book's post-port SELLs are
its *winning* side (10 trades, +$4.91, 4W/6L) and the BUYs are flat (9 trades,
−$0.93). Small n both ways and a different population (86 census signals vs 19
trades) — **no side-based change.**

---

## 5. The BE ratchet: still not adopted, and BTC's value is not gold's 0.75R

Gold's review found its own 0.30R ratchet was armed by noise and raised it to
0.75R. BTC has **no** ratchet, so the honest question is not "copy 0.75R" but
"does a ratchet help BTC, and at what trigger?" Both views, net of $0.25:

**Real trades, 19 log-covered, 240-min horizon (cascade-ignorant):**

| BE trigger | W/L/BE/T | WR | P/L |
|---|---|---|---|
| off (live) | 6/12/0/1 | 33.3% | +$0.14 |
| +0.50R | 5/7/7/0 | 41.7% | +$4.99 |
| +0.75R | 6/10/3/0 | 37.5% | −$1.41 |
| +1.00R | 6/9/4/0 | 40.0% | +$1.19 |
| +1.25R | 5/9/5/0 | 35.7% | +$0.46 |

**Cascade-aware takeable census (78 signals, 1 position + escalating cooldown +
3-SL daily halt), net of $0.25:**

| BE trigger | taken | W/L/BE/T | WR | P/L |
|---|---|---|---|---|
| off (live) | 25 | 8/16/0/1 | 33.3% | +$5.98 |
| +0.50R | 30 | 6/11/12/1 | 35.3% | +$4.02 |
| +0.75R | 30 | 7/14/8/1 | 33.3% | +$1.50 |
| **+1.00R** | 29 | 9/14/5/1 | 39.1% | **+$8.69** |
| +1.25R | 28 | 9/16/2/1 | 36.0% | +$6.72 |

What to take from this:

- The **direction matches gold**: a tight ratchet (0.5–0.75R) loses to no
  ratchet. BTC's own 0.75R row is the *worst* in both views — gold's adopted
  constant is a gold-specific optimum, not a universal one.
- The **magnitude does not clear the bar**: the best row (+1.0R) beats off by
  **$2.71 on 29 cascade trades**, and the two views do not even agree on which
  trigger is best (+0.50R wins on the 19 real trades, +1.00R on the cascade
  census). That is 2–3 trades of noise, not evidence.
- **Verdict: status unchanged from HANDOFF §7(a) — not adopted.** If a live
  trigger is ever armed, use +1.0R, and all tools are already BE-aware so the
  ledger can record a `BE` exit reason. Re-check when the live era has 30+
  trades *and* the real spread is known (a ratchet changes the cost/R balance).

---

## 6. Risk gates: the daily halt is protective, the cooldown is still unmeasured

`phantom_trades.py` (rewritten onto `replay_lib`, 24h horizon, TIME
mark-to-market, `--spread`) over the 70 skipped signals. Gross is what the
blocked signal would have made before cost; net charges one $0.25 round trip per
scored phantom:

| gate | blocked | scorable in log | phantom gross | net of $0.25 |
|---|---|---|---|---|
| daily-halt | 18 | 18 | −$4.93 (3W/15L) | **−$9.43** |
| blackout | 9 | 6 | +$8.27 (3W/3L) | +$6.77 |
| cooldown | 37 | **4** | +$0.84 (2W/2L) | −$0.16 |
| ATR floor | 6 | 0 | — | — |
| **total** | 70 | 28 | +$4.18 | **−$2.82** |

The cost column matters for the same reason §1 exists: at $0.25, the whole
blocked stream is net **negative**, and the daily halt looks *twice* as
protective.

- The 3-SL daily halt is **saving** money (−$4.93 of would-be losses) — keep it.
- Blackout leaks ~$8 on six samples; nothing to act on.
- **Cooldown remains the biggest unmeasured gate**: 33 of 37 cooldown blocks
  still predate the log (which starts 09-07 20:20). Sequential dedup view
  (one position at a time) gives 16 taken, 8W/8L: **+$16.82 gross, +$12.82 net**
  — an estimate, not a fact. No change until the log covers the skips.

---

## 7. The 09-11 → 09-14 collapse is regime, not parameters

Nineteen live-era trades, last ten 1W/9L. Day anatomy from the log:

| day | day range | ÷avg ATR | census signals → outcomes | book |
|---|---|---|---|---|
| 09-12 | 446 | 15.0× | 21 (19 SELL, 2 BUY) → **21 SL** | 2 trades, 0W/2L |
| 09-13 | 950 | 20.8× | 11 → 10 SL, 1 TP | 3 trades, 0W/3L |
| 09-14 | 3,203 | 26.2× | 16 → 7 BUY TP, 7 BUY SL, 2 SELL SL | 4 trades, 1W/3L |

On 09-12 the engine fired 19 SELL signals into a drifting range with a median
ATR of **$29** — a 2×ATR stop of **$58** against hourly candles whose ranges
ran $52–$234 (median $107). Every one of the 21 signals stopped. On 09-14 the same funnel fired
14 BUY signals into a $3,203 range and went 7 TP / 7 SL; the book caught only 4
of them (1W/3L) because the cooldown/halt clipped the sequence.

So the recent bleeding has a mechanism, and it is the same friction the rest of
this review keeps hitting: **the stop distance is calibrated to the current ATR,
but the noise it must survive is the day's structure — and the cost is set by
the spread.** Both §1 (cost/ATR) and this section (noise/ATR) argue for trading
only when ATR is large relative to frictions, which is a very different (and
defensible) rule from "raise the ATR floor because low-ATR trades won less".

For completeness, "does the day have room for a 2R target at all?" (day range ÷
6×ATR): 09-07 **0.99**, 09-08 3.23, 09-09 3.43, 09-10 3.96, 09-11 5.94,
09-12 2.54, 09-13 4.47, 09-14 4.61, 09-15 **0.98**. On the two thinnest days a
single SL+TP journey barely fits in the whole day.

---

## 8. Data hygiene

- **The ledger spans two geometries** and every pooled R statistic is invalid
  across them: before 09-06 17:49 it is SL 1.5×ATR / TP 2.5×ATR (RR 1:1.67,
  breakeven WR 37.5%) with candle fills; from 09-06 17:49 it is the live
  2×/4× (1:2, 33.3%). `check_data.py` now censuses both eras, warns on mixed
  geometry, and sanity-checks realized R (±0.25R) on live-era rows only;
  `replay_lib.planned_rr/geometry/realized_r/GEOM2_DEPLOY` are the single
  source of truth.
- **Exits fill on ticks, not levels.** 0 of 36 live-era exits landed on an M5
  boundary. Median SL overshoot is −0.110 ATR (≈5.5% of risk); median TP
  overshoot +0.128 ATR (max +0.520). Two rows (#40 −1.38R, #52 −1.38R) are
  fast-move tick timing — **not** lot/size/schema drift. `analyze_losers.py` §5
  skips pre-09-06 17:49 rows by design for this reason.
- **Costs are not in the ledger at all.** Every P/L in this repo is gross;
  §1 is the missing half.
- Bar census: 2,098 log rows, 0 duplicate minutes, **one missing M5 slot
  (09-14 08:45)**. `check_data.py` now flags this class.
- Two 09-03 outliers (−$394.84, 100× lot) remain in `trades.csv`; they are
  excluded explicitly (never silently) everywhere.

---

## 9. Verdicts on `docs/HANDOFF.md` §7

| queued item | verdict |
|---|---|
| (a) BE ratchet | **Not adopted.** Direction matches gold (tight is worse), magnitude ~2 trades on n≈20–30. Best row +1.0R; gold's 0.75R is the worst row here. |
| (b) direction-aware London blackout | Not adopted — 6 scorable blackout phantoms, +$8.27 gross. Keep collecting. |
| (c) cooldown softening | Still unmeasured (4 of 37 scorable). No change. |
| (d) raise `MIN_ATR_PERCENT` on WR grounds | **Rejected** (§2 — day proxy + pre-era only). Replaced by the cost-ratio rule (§1). |
| (e) trend-side-only daily breaker | No — the hard halt is protective (−$4.93 phantom). |
| (f) port `pathwalk_sims` + `analyze_losers` | **Done**, plus `win_rate_report.py` and a shared `replay_lib.py`; both gold tools' known bug classes are fixed (SELL inversion, self-validating exit tables, zero-division on ratcheted rows). |
| (g) RSI ≥ 45 (surfaced by gold's review) | **Do not port** (§3 — reverses within-day). |

---

## 10. Tooling changed this cycle

- **new `tools/replay_lib.py`** — one walk core, geometry-aware ledger loader,
  cascade replay, census reconstruction, Wilson intervals.
- **new `tools/pathwalk_sims.py`** — validation-first exit replay; grids A/B
  (real trades, TP + BE), C (cascade-aware), D (raw census), **E (takeable
  census through blackout + cascade + daily-halt — the honest version)**.
- **new `tools/win_rate_report.py`** — baseline/era geometry/slices/durations/
  exit grid/census/filter lab **with a within-day control per candidate**/cost
  sensitivity.
- **new `tools/analyze_losers.py`** — MAE/MFE, winner-vs-loser features, stop
  grid, live-era realized-R sanity.
- **rewritten `tools/phantom_trades.py`** — now on `replay_lib` (24h horizon,
  TIME mark, `--since`, sequential in-trade/cooldown dedup, unscorable rows
  reported instead of hidden).
- **`tools/check_data.py`** — geometry-era census, realized-R sanity, bar
  census, inclusive gap bounds.
- **`tools/smoke_test.py`** — Scenario I locks the walk rules (SELL direction,
  mirror symmetry, SL-first tie-break, ratchet trigger, horizon, cascade).
  Verified by re-introducing gold's SELL-inversion bug into `replay_lib.walk`:
  the suite fails (exit 1) with three Scenario-I assertions, so `autosync.sh`'s
  deploy gate now covers this bug class. Scenario D also gained the busy-day
  daily-loss checks.
- **`trade_filter.py`** — `DAY_WINDOW = 400` for the daily-loss breaker; a
  30-row window silently under-counted busy days (gold hit 62 trades/day).

---

## 11. Next cycle (what would confirm or kill this)

1. **Measure the real XM BTCUSD spread** (MT5 spec + tick log). Then set, or
   refuse to set, a cost-ratio ATR floor. Until this exists, headline P/L
   numbers here are gross and therefore optimistic.
2. **Instrument the two surviving filter candidates** (wick ≤ 0.40, near-EMA
   ≤ 0.15 ATR) as log-only counters; re-run the own-day control at n ≥ 30.
3. **Re-run the era report at 30+ live-era trades** and only then decide the
   +1.0R ratchet. Watch whether the +0.75R anomaly persists.
4. **Let the log grow** so `phantom_trades.py` can score the cooldown blocks
   (33 of 37 are still pre-log). No cooldown change before that.
5. Keep the SELL/BUY census-vs-book conflict on the watch list rather than
   acting on either side.
6. Data: keep `forward_test_log.csv` continuous (one missing 09-14 08:45 slot).

### What would change the conclusions

- A measured spread **< $0.10** would make the current stream economically
  viable and move the whole cost discussion down the priority list.
- 30+ live-era trades at ≥ 45% WR would make the ratchet and filters moot.
- A live-era run of the wick/near-EMA candidates at 30+ samples that keeps the
  own-day agreement would justify a gate change with BTC evidence.

---

## 12. Re-cut 2026-09-16 (78 trades) — what moved

Appended one day later, after `tools/autosync.sh` brought the ledger to 78
closed trades (data commit `data collection 601`, engine state 2026-09-16 12:10
UTC, no open trade). **Where this section and the body disagree, this section is
current.** The question did not change, so per `docs/HANDOFF.md` §10 the review
was re-cut in place instead of starting a new file.

Reproduce (note `--spread 0.40`, the measured cost — the body above was written
at `--spread 0.25` before the terminal measurement in §1b):

```bash
python3 tools/handoff_check.py                        # is the handoff snapshot true?
python3 tools/check_data.py                           # 0 fail, 61 warn
python3 tools/win_rate_report.py --spread 0.40
python3 tools/pathwalk_sims.py --spread 0.40 --census # agreement 24/24
python3 tools/analyze_losers.py --spread 0.40
python3 tools/validate_gates.py                       # gross
python3 tools/phantom_trades.py --spread 0.40
python3 tools/smoke_test.py                           # SMOKE TEST PASSED
```

### 12.1 The book

| Slice | n | W/L | WR | gross | net @$0.40 | net/trade |
|---|---|---|---|---|---|---|
| all rows (incl. 09-03 outliers) | 78 | 33/45 | 42.3% | −374.39 | −405.59 | −5.20 |
| from 09-05 (excl. outliers) | 76 | 33/43 | 43.4% | **+20.45** | **−9.95** | −0.13 |
| live geometry (≥09-06 17:49) | 41 | 18/23 | 43.9% | +19.23 | +2.83 | +0.07 |
| new regime as the tools slice it (≥09-09 02:25) | 24 | 10/14 | 41.7% | **+17.44** | **+7.84** | +0.33 |
| strictly post-gate (≥09-10) | 20 | 8/12 | 40.0% | +14.68 | +6.68 | +0.33 |
| last 10 | 10 | 4/6 | 40.0% | +6.26 | +2.26 | +0.23 |

Body figures for comparison: 71 trades from 09-05 were +$11.74 gross / −$16.66
net; the live era (n=19) was +$8.73 gross / +$1.13 net, and −$10.29 net without
09-10. **Five trades (09-15 → 09-16: 3W/2L, +$8.71 gross) moved the from-09-05
slice from −$16.66 to −$9.95 net and the live era from +$1.13 to +$7.84 net.**
Excluding 09-10 the live era is now n=21, 7W/14L, +$4.82 gross / **−$3.58 net**
(was −$10.29 net) — still negative, much less dependent on one day.
True equity from $200 is −$174.39 against an engine ledger of $217.45
(drift +$391.84, unchanged in character).

### 12.2 The cost finding is unchanged in shape, closer in margin

Live-era median entry ATR is **$87.85** (was $84) ⇒ 1R = **$1.757** ⇒ the $0.40
round trip is **22.8% of 1R** (was 23.9%) ⇒ breakeven decisive WR **40.9%**
(was 41.3%) against an actual **41.7%** (was 36.8%; Wilson 24.5–61.2). The book
has crossed from "below its cost-adjusted breakeven" to "at it", on 24 trades.
§1's conclusion stands: at this ATR the strategy is one spread widen away from
being a fee stream, and the timeframe re-cut (M5 24.0% / M15 11.9% / H1 5.4% /
H4 2.6% of 1R) is still the decision that dominates every parameter below.

### 12.3 Era boundary correction — the gates went live on 09-10, not 09-09

`replay_lib.PORT_DEPLOY = 2026-09-09 02:25` is the first trade of the slice this
review calls "new regime", but two pieces of evidence put the *gated code* a day
later:

- the ledger's first SELL is **09-10 09:05** (#59) — the SELL funnel arrived
  with the port, and every trade before it is a BUY;
- two of the four 09-09 BUY rows violate the near-EMA gate that the port
  installed: **#57 sits 3.11 ATR** and **#58 0.57 ATR** on the adverse side of
  `EMA50_At_Entry` (`MAX_BELOW_EMA_ATR = 0.30` would have blocked both).

So `replay_lib.GATES_DEPLOY = 2026-09-10 00:00` was added (mirrored by
`check_data.GATES_LIVE`) for anything that **re-applies an entry rule to ledger
rows**. Era *slices* keep using `PORT_DEPLOY` so the numbers already published
here stay comparable. The strictly-post-gate slice is n=20, 8W/12L, +$14.68
gross / +$6.68 net — identical to "last 20" at this snapshot.

`tools/check_data.py` gained an **entry-gate conformance** block that re-applies
today's trend / RSI / near-EMA rules to every row from `GATES_LIVE` on using only
ledger fields: **all 20 pass, 0 new warnings**. Rows before it are counted, not
judged. This is the check that would have caught an unrecorded param change or a
silently-skipped gate — the two failure modes this project cannot see otherwise.

### 12.4 Filter candidates re-graded

| Candidate | §9 verdict (73 trades) | verdict now (78 trades) |
|---|---|---|
| ATR% ≥ 0.06 | rejected — day proxy | **unchanged** (own-day keep collapses to n=4, 0.0%, −7.48) |
| RSI ≥ 45 | do not port — reverses within-day | **unchanged** (own-day keep 50 signals, 22.0%, −35.82) |
| momentum-aligned | not adopted | **unchanged** (own-day keep 41, 19.5%, −27.26) |
| wick ≤ 0.40 | survived own-day control, needs n≥30 | **demoted → unresolved**: the census and the ledger now disagree on the *sign* (census keep 56 35.7% **+2.46** vs skip 36 27.8% −11.11; ledger keep 26 42.3% **−6.42** vs skip 50 44.0% −3.53; `analyze_losers` winners' wick 62.1% vs losers' 47.4%). A feature whose direction flips between views is not a candidate, it is noise with a story |
| near-EMA "≤0.15 ATR" | survived own-day control, needs n≥30 | **the label was wrong — corrected.** It measured a **two-sided** band while naming `MAX_BELOW_EMA_ATR`, which is **one-sided**. Measured separately (§12.5) |
| side = SELL | watch list | **unchanged, sharper**: book SELL n=15 7W/8L +16.12 gross / +10.12 net vs BUY n=9 3W/6L +1.32 / −2.28; census SELL 22W/51L (30.1%) −12.20 net vs BUY 10W/15L (40.0%) +3.55. The funnel fired 75 SELL vs 25 BUY signals |

### 12.5 The EMA50-distance correction (this cycle's main finding)

`win_rate_report.py` §7 now prints both quantities separately, on the census and
on the post-gate ledger rows.

**One-sided adverse distance — the live gate. Tightening it hurts, in both views.**

| `MAX_BELOW_EMA_ATR` | census (92 takeable, net) | post-gate ledger (n=20, net) |
|---|---|---|
| **0.30 (live)** | keep 92 · **−8.66** | keep 20 · **+6.68** (nothing violates it) |
| 0.15 | keep 85 · **−11.20** | keep 18 · +5.49 (blocks 2 winners worth +1.19) |
| 0.00 | keep 80 · **−18.23** | keep 18 · +0.07 |

In the live era a 0.15 gate would have blocked **5 trades worth +$4.57 net** and
a 0.00 gate **6 worth +$7.77 net**. The previous handoff carried this as a queued
candidate ("entry within 0.15 ATR of EMA50 (candidate: tighten
`MAX_BELOW_EMA_ATR`)"); acting on it as written would have cost money. It is now
in §7's "Explicitly NOT queued".

**Two-sided band |entry − EMA50| ≤ K — a NEW gate, and the strongest survivor.**

| K | census keep (net) | census own-day | post-gate ledger keep (net) | live era keep (net) |
|---|---|---|---|---|
| 0.15 | 9 · 55.6% · +9.49 | drops 38 | — | 4 · 3W/1L · +8.87 |
| **0.50** | 25 · 44.0% · **+10.46** vs skip 67 · 28.4% · −19.11 | **drops 0** | 5 · 80.0% · **+11.97** vs skip 15 · 26.7% · −5.29 | 6 · 5W/1L · **+16.45** vs skip 18 · −8.61 |
| 1.00 | 48 · 41.7% · +15.67 (own-day keep +23.17) | drops 3 | 8 · 75.0% · +16.99 vs skip 12 · 16.7% · −10.31 | 10 · 7W/3L · +16.48 vs skip 14 · −8.64 |

K = 0.50 is the only candidate in the whole lab whose within-day control drops
**zero** signals — every day in the log contains both a keep and a skip, so the
split cannot be a calendar proxy — and it agrees in sign with the ledger and the
live era. The keep-set is n=5–25 depending on the view, so this is
**instrument-and-watch, not gate** (`docs/HANDOFF.md` §7.4). It is also free to
instrument: `EMA50_At_Entry`, `ATR_At_Entry` and `Entry_Price` are already in
every ledger row, so no engine change is needed to keep scoring it.

### 12.6 Exits: the picture moved, still not actionable

`pathwalk_sims.py --spread 0.40 --census`, 24 log-covered trades, replay
agreement **24/24** (was 19/19 at 73 trades).

| Rule | 24 real trades | cascade census (92 takeable) | all 100 signals |
|---|---|---|---|
| TP 3×ATR | +6.79 | +2.89 | −9.59 |
| **TP 4×ATR (live)** | **+3.97** | **+7.26** | −8.65 |
| TP 5×ATR | +12.50 | +12.65 | −1.05 |
| TP 6×ATR | +16.60 | +19.43 | — |
| BE off (live) | +3.97 | +7.26 | −8.65 |
| BE +0.50R | +10.90 | +4.09 | −7.94 |
| BE +0.75R | +4.49 | +4.11 | −3.77 |
| **BE +1.00R** | +7.10 | **+11.46** | **+6.00** |
| BE +1.25R | +6.36 | +9.63 | — |

- **Wider TP now beats the live 4×ATR in both cascade-aware views** (5×ATR:
  +12.50 / +12.65; 6×ATR: +16.60 / +19.43). At 73 trades this direction did not
  exist. It is a few dollars on n=24–30 and it lengthens holds (fewer cascade
  slots), so it stays **deferred behind the timeframe decision** — but it is no
  longer "no evidence", it is "weak consistent evidence".
- **+1.0R remains the only BE row positive in all three views.** §5's verdict
  (not adopted; if ever armed, use +1.0R) stands, with one correction: +0.50R is
  now the best row on the 24 real trades (+10.90) while *losing* to "off" in the
  cascade census (+4.09 vs +7.26) — a disagreement, i.e. noise. `analyze_losers`
  explains the tension: 6 of 14 eventual losers reached +0.50R first, and a
  +0.50R ratchet would arm on those 6 **and on 10/10 eventual winners**.
- Gold's adopted +0.75R is still the worst-or-near-worst row on BTC in every
  view. The port decision (§5, §9(a)) is unchanged.
- `analyze_losers.py` §6's stop-width grid (SL 1.0× → 3.0× with TP fixed at 4×)
  is a **geometry change, not a free parameter** — SL 1.0×/TP 4× is RR 1:4, so its
  +17.53 is not comparable to the live +3.97. Read §A/§B of `pathwalk_sims.py`.

### 12.7 Blocked signals and fills

- Phantom replay (`--spread 0.40`, 73 skips, 31 scorable): daily-halt 18/18 →
  3W/15L, −4.93 gross / **−12.13 net (protective, unchanged)** · blackout 6/9 →
  +8.27 / **+5.87 (costly)** · cooldown **7/40** (was 4/37) → +6.72 / **+3.92
  (costly — the sign flipped since §6)** · ATR floor 0/6. Totals +10.06 gross /
  −2.34 net; sequential view 17 taken, 8W/9L, +14.94 gross / +8.14 net.
  §6's conclusion ("the cooldown's cost is unmeasured") is now half-obsolete:
  **7 blocks score, and they score against the cooldown.** 33 still predate the
  log — no change before that grows.
- Gate replay (`validate_gates.py`, gross, log-covered tail): `SLOPE30` 23 kept
  9W/14L +13.15 · `ABV50s` 22 kept 9W/13L +17.74 · `RISE120` 19 kept 8W/11L
  +14.73 · `PROX1.0` 17 kept 10W/7L +24.82 · `PROX0.5` 7 kept 5W/2L +13.75 ·
  adopted combo 22 kept 9W/13L +17.74. Every adopted gate is still positive on
  what it can score; the kept sets now contain more losses than wins and are
  carried by the 1:2 payoff.
- Fills: a **third** overshoot row appeared — **#65 SELL 09-12 04:25, −1.22R**
  (−22%) — alongside #40/#52 at −1.38R. SL rows slip median −5.0% of 1R (worst
  −37.9%), TP rows overshoot median +2.2% (worst +12.2%). Still tick timing in
  fast moves, not sizing drift; still the reason the walk uses an SL-first
  tie-break.
- Bar census: **2494 log rows** (was 2098), 0 duplicate minutes, **three** missing
  M5 slots (09-14 08:45, 09-15 03:45, 09-15 13:00). Coverage is now 24 of 78
  trades; the 54 pre-log rows are why every log-joined table above is a statement
  about the last ~24 trades.
- Regime context for the new rows: at the snapshot the funnel had evaluated 382
  candles since the last restart with BUY `trend_confirmed = 0` (EMA50 below
  EMA200 throughout) and 7 full SELL signals — the engine has been short-only,
  which is why 09-15/09-16 are all SELL. Funnel counters are since-restart, not
  lifetime (`docs/HANDOFF.md` §9).

### 12.8 Tooling changed this cycle

- **new `tools/handoff_check.py`** — machine-checks `docs/HANDOFF.md`'s snapshot
  block against the live CSVs (`HANDOFF FRESH` / `HANDOFF STALE`), refuses to
  let a command block quote a different `--spread` than the snapshot's cost
  assumption, verifies that referenced files exist, and rewrites the block with
  `--update`. Exit 0/1/2; advisory everywhere.
- **`tools/autosync.sh`** — new phase 4b runs that check and adds a
  `📝 handoff:` line to the Telegram digest (never blocks a deploy, never
  touches the 🚨/⚠️ paths). Documented in `docs/AUTOSYNC.md` §3.
- **`tools/check_data.py`** — entry-gate conformance block (§12.3).
- **`tools/win_rate_report.py`** — the EMA50-distance candidates are now printed
  as two labelled quantities (two-sided band vs one-sided live gate) on both the
  census and the post-gate ledger rows; the one-sided rows suppress the
  within-day control (a near-universal gate makes it pure composition noise);
  the hard-coded "71 real trades" label is computed.
- **`tools/replay_lib.py`** — `GATES_DEPLOY` added next to `PORT_DEPLOY`, with
  the evidence for the split in its comment.

### 12.9 Next cycle (supersedes §11 where they differ)

1. Bar timeframe decision first (§1 / §12.2) — M5 is at, not above, its
   cost-adjusted breakeven, and every other parameter is a function of 1R.
2. Spread profile by session (the $40.00 is one Asian-session sample).
3. 30+ live-era trades, then re-cut §12.1–§12.6; decide wider TP and BE +1.0R
   there, not before.
4. Instrument the **two-sided** EMA50 band (log/skip rows) — and leave
   `MAX_BELOW_EMA_ATR` alone.
5. Grow log coverage so the 33 remaining cooldown blocks and 3 blackout blocks
   become scorable; the cooldown currently reads costly on 7.
6. Keep the daily halt, the 1:2 geometry, and the hard blackouts. Reconcile the
   SELL-book-vs-SELL-census conflict with a gated cascade replay per side.


## 13. Re-cut 2026-09-24 (99 closed trades) — the 30-trade milestone is met

Autosync data is current through **2026-09-23 20:35 UTC** (`data collection
1301`); `status.json` reports 99 closed rows and an open simulated SELL #99.
This section supersedes §12 where the samples overlap. The question has not
changed, so the review is re-cut in place rather than starting a new file.
No code, parameter, or execution-mode change is part of this review.

Reproduce from the repository root:

```bash
python3 tools/handoff_check.py                         # HANDOFF FRESH after §10 update
python3 tools/check_data.py                            # 0 fail, 62 warn
python3 tools/win_rate_report.py --spread 0.40
python3 tools/pathwalk_sims.py --spread 0.40 --census  # 45/45 actual-outcome agreement
python3 tools/analyze_losers.py --spread 0.40
python3 tools/validate_gates.py                        # retrospective; gross
python3 tools/phantom_trades.py --spread 0.40
python3 tools/smoke_test.py                            # engine regression check
```

Every net figure below uses the measured **$0.40 round trip**. `trades.csv`
contains gross results only. Do not pool R totals or breakeven rates across the
2026-09-06 17:49 geometry change.

### 13.1 The book and the correct era boundaries

| Slice | n | W/L | WR | gross | net @$0.40 |
|---|---:|---:|---:|---:|---:|
| All ledger rows (includes the two 09-03 sizing outliers) | 99 | 44/55 | 44.4% | −$342.41 | **Do not use**: the 09-03 lot size makes a flat $0.40 haircut invalid |
| From 09-05 (outliers excluded) | 97 | 44/53 | 45.4% | +$52.43 | **+$13.63** |
| Live 2×/4× geometry (≥09-06 17:49) | 62 | 29/33 | 46.8% | +$51.21 | +$26.41 |
| Post-port tool slice (≥09-09 02:25) | 45 | 21/24 | 46.7% | +$49.42 | +$31.42 |
| Strictly post-gate (≥09-10 00:00) | 41 | 19/22 | 46.3% | +$46.66 | +$30.26 |
| Since prior snapshot time (≥09-17 04:05) | 19 | 11/8 | 57.9% | +$37.59 | +$29.99 |
| Last 10 rows | 10 | 7/3 | 70.0% | +$28.45 | +$24.45 |

The 09-09 port slice has four entries from before the gates went live; use the
**41-row ≥09-10 slice** to evaluate the current gates. The 19 newer rows explain
the engine-ledger increase of $37.59 since the prior snapshot; after the modelled
$0.40/trade cost their net is +$29.99. The last-10 result is concentrated in a
high-ATR patch: median ATR is $131.80 there, versus $89.58 on the strict
post-gate sample. Larger dollar moves and a lower spread-to-risk ratio therefore
contribute to the short-window P/L; this is not evidence of a parameter effect.

The raw 99-row Profit sum is −$342.41 gross, or −$142.41 from the $200 start
before spread costs. `status.json`'s engine ledger is $249.43, a known +$391.84
discrepancy from the old oversized 09-03 rows and pre-port balance resets. This
is an accounting-history issue, not a fresh autosync failure; exclude the
outliers from strategy conclusions.

### 13.2 Cost-adjusted evidence: positive point estimate, not conclusive

On the 41 strictly post-gate rows, median entry ATR is **$89.58**, so median
1R is about **$1.79** at 0.01 lot. A $0.40 round trip is **22.3% of median 1R**;
for the live 1:2 geometry that implies an approximate decisive breakeven WR of
**40.8%**. Observed WR is **46.3%**, but the Wilson 95% interval is
**32.1–61.3%** and still includes that threshold. The post-port 45-row slice is
46.7% (Wilson 32.9–60.9). This says the current system remains plausible, not
that its edge is established.

For context, the last 10 are 7W/3L and +$24.45 net, with median ATR $131.80;
the spread is only about 15.2% of the median 1R in that window. The $0.40
spread measurement is still one XM Asian-session sample (09-15 04:06 UTC). No
new M15/H1 replay or London/NY spread sample was added, so timeframe and
session-cost decisions remain open.

### 13.3 Side results: BUY now leads in both views, but don't gate SELL off

| Strict post-gate book | n | W/L | WR (Wilson 95%) | gross | net @$0.40 |
|---|---:|---:|---:|---:|---:|
| BUY | 21 | 11/10 | 52.4% (32.4–71.7) | +$34.12 | +$25.72 |
| SELL | 20 | 8/12 | 40.0% (21.9–61.3) | +$12.54 | +$4.54 |

The unfiltered 216-signal census at live geometry also favours BUY (+$22.00 net)
over SELL (−$37.60 net). This is a directionally consistent change from §12,
where the book and census were in tension. It is not a side-filter result:
the census ignores cascade and risk-gate selection, while the book contains
only taken trades; each actual side still has n≈20. Keep both directions while
collecting a larger gated sample.

### 13.4 Signal census and entry-gate candidates

`win_rate_report.py` reconstructs **216 full signals** from the log (100 BUY,
116 SELL); 26 are in blackout windows, leaving 190 takeable. At live geometry,
the takeable but otherwise unfiltered stream is 53W/117L/20T and **−$32.94
net**. The all-216, blackout-inclusive, cascade-ignorant pathwalk is 68W/126L/22T
and **−$15.60 net**. Those are different universes, not contradictory
estimates of the traded book. Applying one-position-at-a-time, cooldown, daily
halt and blackout replay to the takeable census selects 52 TP4 trades
(19W/28L/5T, **+$16.69 net**); that is a model, not 52 extra fills.

Current filter read, all counterfactual P/L net of $0.40. Census rows are the
190 takeable signals; ledger-feature comparisons use the 97 post-outlier trades
unless explicitly marked post-gate:

| Candidate | Current reading | Decision |
|---|---|---|
| ATR% ≥0.06 | Census pooled keep +$2.51 vs skip −$35.45, but own-day keep −$23.82 vs skip −$14.50; ledger keep +$36.67 vs skip −$23.04 | Still day-confounded across views; do not adopt |
| RSI ≥45 | Census keep −$25.99, skip −$6.95; own-day keep −$28.39. Ledger split is positive on both sides (+$11.17 / +$2.46) | Still unsupported; do not port from gold |
| Wick ratio ≤0.40 | Census keep 108 +$3.74 vs skip 82 −$36.68, but ledger keep 39 −$3.09 vs skip 58 +$16.72 | **Unresolved:** sign conflict |
| Two-sided EMA50 band | Census ≤0.15 ATR: keep 22 +$15.41 vs skip 168 −$48.35; post-gate ledger ≤0.50 ATR: keep 15 (10W/5L) +$27.88 vs skip 26 (9W/17L) +$2.38 | **Monitor/log only:** different cutoffs and small samples; not a live gate |
| Tighten one-sided `MAX_BELOW_EMA_ATR` 0.30→0.15/0.00 | Census gets worse as it tightens; post-gate ledger tightening to 0.15 blocks two positive rows worth +$1.19 net | Rejected; do not conflate with the two-sided band |

The actual live gate is one-sided adverse-side distance only. Census and ledger
filters are retrospective selection analyses; even consistent splits are not
prospective evidence. The two-sided EMA distance can be calculated for logged
trades, but future blocked signals need richer per-signal/skip logging to make
them comparable.

### 13.5 Exit replay: wider TP is worth monitoring, not adopting

`pathwalk_sims.py` matches actual outcomes for **45/45 log-covered trades**
inside their actual exit windows. The following counterfactuals use a 240-minute
horizon and the $0.40 cost; `TIME` marks are marked to market at the horizon.
The cascade column models one position at a time and risk gates, so counts vary
when holds get longer.

| Rule | 45 covered trade paths (net) | Gated cascade census (net) |
|---|---:|---:|
| TP 4×ATR (live) | +$17.05 (17W/23L/5T) | +$16.69 (52 taken) |
| TP 5×ATR | +$34.73 (17W/23L/5T) | +$31.24 (49 taken) |
| TP 6×ATR | +$38.62 (14W/23L/8T) | +$37.81 (48 taken) |
| BE +0.50R | +$12.68 | −$2.98 (63 taken) |
| BE +0.75R | +$5.84 | +$8.28 (59 taken) |
| BE +1.00R | +$17.52 | +$18.23 (56 taken) |
| BE +1.25R | +$16.13 | +$15.75 (55 taken) |

Wider targets improve both views on this sample, but the real-path and census
results are counterfactual and sensitive to horizon, hold time and cascade.
The +1.0R ratchet is only +$0.47 above live on the covered paths and +$1.54 in
the cascade census. +0.50R and +0.75R do not consistently beat the live rule.
**Keep TP4 and BE off** pending a prospective re-cut; the timeframe decision
still comes first.

### 13.6 Blocked signals, gate replay and fills

Phantom replay has 88 logged skips: 46 are scorable (16W/30L), 42 unscorable,
for +$15.50 gross / **−$2.90 net** overall. By gate:

- Daily halt: 24/24 scorable, 3W/21L, −$16.08 gross / **−$25.68 net** —
  protective; keep it.
- Blackout: 14/17 scorable, 8W/6L, +$19.35 gross / **+$13.75 net** —
  costly-looking, but only n=14.
- Cooldown: 8/41 scorable, 5W/3L, +$12.23 gross / **+$9.03 net**; 33 remain
  unscorable, so this is not grounds to soften it.
- ATR: 0/6 scorable.

The sequential deduplicated estimate is 28 trades, 13W/15L and +$17.88 gross /
+$6.68 net. It ignores full cascades and is an estimate, not evidence. Keep the
current risk stack while coverage grows.

`validate_gates.py` is a historical, gross-only gate replay, not a prospective
backtest. Its adopted SLOPE30+ABV50s+NOH8 combo keeps 43 ledger rows (20W/23L,
+$49.72 gross); no gate change follows from this selected retrospective set.

On 2×/4× geometry, realized SL slip is median −3.8% of 1R (worst −37.9%); TP
overshoot median +2.4% (worst +12.2%). Three SLs exceed 20% of planned risk:
#40 and #52 at −1.38R, #65 at −1.22R. `check_data.py` flags the first two at
its tighter 0.25R threshold; `analyze_losers.py` flags all three at 20%. This
still looks like fast-move/tick fill drift, not a new sizing regression.

### 13.7 Data health and current simulator state

`check_data.py`: **0 fail / 62 warn**, 4611 M5 rows from 09-07 20:20 through
09-23 20:35 UTC, no gap >15 minutes. Six missing M5 slots occur in five runs;
one minute has two same-OHLC rows at 09-23 10:50:00 and 10:50:01 with updated
indicators. This is logged as a re-evaluation warning. Of 99 closed trades,
45 are log-covered and 54 predate the log. Indicator reconstruction matches
floor 4591/4591, ATR 4596/4596, RSI 4596/4596; all 41 strictly post-gate ledger
rows pass trend, RSI and near-EMA conformance.

At the last state, the forward-test simulator has an open SELL #99 entered
09-23 17:55 UTC at 84301.75, SL 84638.45, TP 83628.34; daily losses are 1/3.
The open trade is not included in the 99 closed-row sample. This is simulated
forward testing, not realized broker P/L.

### 13.8 Verdict and next cycle

1. The 30-trade milestone is met, but 41 strict post-gate trades are not enough
   to establish an edge; the cost-adjusted WR interval still spans breakeven.
2. Keep the current 2×/4× geometry, TP4, no BE ratchet, daily halt, cooldown,
   blackouts, and entry gates unchanged. No code or parameter change is justified
   by this review.
3. Prioritize the same-rule M15/H1 comparison and spread samples across sessions.
4. Keep BUY and SELL active; collect a larger gated sample per side rather than
   applying the raw census as a directional filter.
5. Continue logging until more cooldown/blackout skips are scorable; keep the
   two-sided EMA50 distance and wick ratio monitor-only.
6. Revisit TP5/TP6 and BE +1.0R only with a later prospective sample and after
   the timeframe/cost decision. Do not infer adoption from these 240-minute
   counterfactuals.
